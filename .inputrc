set editing-mode vi

$if mode=vi
    # normal mode
    set keymap vi-command
    "gg": beginning-of-history
    "h": backward-char
    "l": forward-char
    "G": end-of-history
    # insert mode
    set keymap vi-insert
    "jk": vi-movement-mode # remap escape
$endif
